/**
 * UserController
 * @typedef {import('./user.controller')} UserController
 */

/**
 * UserProvider
 * @typedef {import('./user.provider')} UserProvider
 */

/**
 * UserRoutes
 * @typedef {import('./user.routes')} UserRoutes
 */
